package chess.legal_move_check;

/**
 * write important notes about this implementation
 * write what each function does
 * write what each function returns
 * clarify that each function overrides
 */
public class KnightLegalMoveCheck implements LegalMoveCheck {
    /*
     * Knight can move in the following ways:
     *      LShape()
     * Knight can move so long as his moves must stay within the confines of the board
     */
}
